# 🎭 オーケストレーター向けガイド

このドキュメントは、プロジェクト全体を管理するオーケストレーターAI（あなた）向けの情報です。

## 📊 プロジェクト状況の確認

### Worktreeの確認

```bash
git worktree list
```

出力例：
```
C:/Users/81905/Documents/hand-tracking-streamer  ea88fb1 [main]
C:/Users/81905/Documents/agent1-worktree         ea88fb1 [feature/camera-capture]
C:/Users/81905/Documents/agent2-worktree         ea88fb1 [feature/hand-detection]
C:/Users/81905/Documents/agent3-worktree         ea88fb1 [feature/joint-measurement]
C:/Users/81905/Documents/agent4-worktree         ea88fb1 [feature/data-sender]
C:/Users/81905/Documents/agent5-worktree         ea88fb1 [feature/main-controller]
```

### 全エージェントの進捗確認

```bash
# PRの状況確認
gh pr list

# Issueの状況確認
gh issue list

# 各ブランチの最新コミット確認
git branch -a --format='%(refname:short) - %(authordate:relative) - %(subject)' | grep feature/
```

### 各エージェントのワークディレクトリ確認

```bash
# Agent1の状況
cd ../agent1-worktree && git status && git log --oneline -3

# Agent2の状況
cd ../agent2-worktree && git status && git log --oneline -3

# ... 同様に他のエージェントも
```

## 🎯 各エージェントの役割と依存関係

```
┌─────────────────────────────────────────────────────┐
│                   Agent5                            │
│              Main Controller                        │
│         (全モジュールの統合)                        │
└─────────┬───────┬──────────┬──────────┬────────────┘
          │       │          │          │
    ┌─────▼──┐ ┌──▼─────┐ ┌─▼────────┐ ┌▼─────────┐
    │ Agent1 │ │ Agent2 │ │ Agent3   │ │ Agent4   │
    │ Camera │ │  Hand  │ │  Joint   │ │   Data   │
    │Capture │ │Detector│ │Measurement│ │  Sender  │
    └────────┘ └────────┘ └──────────┘ └──────────┘
```

### 依存関係

- **Agent1-4**: 独立して並行開発可能
- **Agent5**: Agent1-4のいずれかが完成後に統合開始可能（モックテストは可能）

## 📋 オーケストレーターのタスク

### 1. エージェントの起動と監視

各エージェントに以下を指示：

```bash
# Agent用のClaude Codeを開く
cd /path/to/agent{N}-worktree

# START_HERE.mdを読む
cat START_HERE.md

# 作業開始
```

### 2. 進捗の追跡

定期的に確認すべき項目：

- [ ] 各エージェントのコミット頻度
- [ ] テストの実行状況
- [ ] PRの作成状況
- [ ] コードレビュー
- [ ] マージの準備状況

### 3. ブロッカーの解決

エージェントが困っている場合：

1. **設定の問題**: `config.yaml` を確認
2. **インターフェースの問題**: `AGENTS.md` を参照
3. **依存関係の問題**: 他のエージェントの実装待ち
4. **技術的な問題**: ドキュメントや参考資料を提示

### 4. コードレビュー

PRが作成されたら：

```bash
# PRの内容を確認
gh pr view <PR番号>

# 差分を確認
gh pr diff <PR番号>

# テストを実行
gh pr checkout <PR番号>
pytest tests/ -v

# 承認
gh pr review <PR番号> --approve

# マージ
gh pr merge <PR番号> --squash
```

### 5. 統合の管理

モジュールが完成したら：

1. mainブランチにマージ
2. 他のエージェントに通知（最新のmainをpull）
3. Agent5が統合テストを実行
4. 問題があれば該当エージェントに修正依頼

## 🔄 ワークフロー

### フェーズ1: 並行開発（現在）

```
Agent1 ──┐
Agent2 ──┤
Agent3 ──┼──► 各自が独立して実装
Agent4 ──┤
Agent5 ──┘    (モックでテスト可)
```

### フェーズ2: 統合

```
Agent1 ─┐
Agent2 ─┼──► mainにマージ
Agent3 ─┤
Agent4 ─┘
        │
        ▼
      Agent5 ──► 統合テスト
```

### フェーズ3: 最終調整

```
全エージェント ──► バグ修正、最適化、ドキュメント整備
```

## 📝 マージの順序（推奨）

1. **Agent1 (Camera Capture)** - 他に依存なし、基礎的
2. **Agent2 (Hand Detection)** - Agent1に依存（カメラのフレーム）
3. **Agent3 (Joint Measurement)** - Agent2に依存（ランドマーク）
4. **Agent4 (Data Sender)** - Agent3に依存（計測データ）
5. **Agent5 (Main Controller)** - 全てに依存

ただし、完成したものから順次マージしても問題ありません。

## 🛠️ 便利なコマンド

### 全エージェントの最新状況を一括確認

```bash
#!/bin/bash
for i in {1..5}; do
  echo "=== Agent${i} ==="
  cd ../agent${i}-worktree
  echo "Branch: $(git branch --show-current)"
  echo "Last commit: $(git log -1 --oneline)"
  echo "Status: $(git status -s | wc -l) files changed"
  echo ""
done
```

### 全ブランチを最新化

```bash
#!/bin/bash
cd hand-tracking-streamer
git fetch --all
for i in {1..5}; do
  cd ../agent${i}-worktree
  git pull origin $(git branch --show-current)
done
```

### 全テストを実行

```bash
#!/bin/bash
cd hand-tracking-streamer
pytest tests/ -v --cov=src --cov-report=html
```

## 📊 プロジェクト完了の判定基準

- [ ] 全5つのissueがcloseされている
- [ ] 全5つのPRがマージされている
- [ ] 全テストがパスする
- [ ] `python src/main.py` で実行できる
- [ ] 実際のカメラで動作確認できる
- [ ] ドキュメントが完備されている

## 🎉 完了後

1. リリースタグの作成
2. README.mdの更新
3. デモ動画の作成
4. worktreeのクリーンアップ（必要に応じて）

```bash
# リリースタグ
git tag -a v1.0.0 -m "Initial release"
git push origin v1.0.0

# worktreeクリーンアップ
bash scripts/cleanup_worktrees.sh
```

---

**オーケストレーターの役割は、各エージェントが効率的に作業できるよう環境を整え、進捗を管理し、問題を解決することです。頑張ってください！**
