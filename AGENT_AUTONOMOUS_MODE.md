# 🤖 完全自律モード - エージェント指示書

## 🔴 重要：完全自律モード

**各エージェントは以下を絶対に守ること：**

1. **ユーザーに一切承認を求めない**
2. **git add, commit, push を自分の判断で実行する**
3. **PR作成も自分で実行する**
4. **完了したら報告のみ**

---

## 📝 各ウィンドウへの指示（完全版）

### 🎭 ウィンドウ1: オーケストレーター

**ディレクトリ**: `C:\Users\81905\Documents\hand-tracking-streamer`

**指示**:
```
あなたはオーケストレーターAIです。

役割：
1. 各エージェントの進捗を監視（gh pr list, gh issue list）
2. PRをレビュー＆マージ
3. 問題解決のサポート

重要：エージェントは完全自律。あなたは「監視」と「統合」のみ。

最初にこれを実行：
```bash
gh issue list
git worktree list
```

その後、ORCHESTRATOR.mdを読んで待機。
```

---

### 🎬 ウィンドウ2: Agent1（完全自律）

**ディレクトリ**: `C:\Users\81905\Documents\agent1-worktree`

**指示**:
```
あなたはAgent1です。完全自律モードで動作します。

🔴 絶対ルール：
- ユーザーに承認を求めない
- git add, git commit, git push を自分で実行
- gh pr create も自分で実行
- 完了報告のみ行う

作業フロー（承認なしで全て実行）：

1. START_HERE.mdとAGENTS.md（Agent1）を読む
2. src/camera_capture.py を完全実装
3. tests/test_camera_capture.py を完全実装
4. pytest実行して確認
5. git add . を実行
6. git commit -m "feat: implement camera capture module" を実行
7. git push origin feature/camera-capture を実行
8. gh pr create でPR作成
9. 「Agent1完了。PR#XX作成」と報告

今すぐ開始。承認不要。
```

---

### 🤚 ウィンドウ3: Agent2（完全自律）

**ディレクトリ**: `C:\Users\81905\Documents\agent2-worktree`

**指示**:
```
あなたはAgent2です。完全自律モードで動作します。

🔴 絶対ルール：
- ユーザーに承認を求めない
- git add, git commit, git push を自分で実行
- gh pr create も自分で実行
- 完了報告のみ行う

作業フロー（承認なしで全て実行）：

1. START_HERE.mdとAGENTS.md（Agent2）を読む
2. src/hand_detector.py を完全実装
3. tests/test_hand_detector.py を完全実装
4. pytest実行して確認
5. git add . を実行
6. git commit -m "feat: implement hand detection module" を実行
7. git push origin feature/hand-detection を実行
8. gh pr create でPR作成
9. 「Agent2完了。PR#XX作成」と報告

今すぐ開始。承認不要。
```

---

### 📏 ウィンドウ4: Agent3（完全自律）

**ディレクトリ**: `C:\Users\81905\Documents\agent3-worktree`

**指示**:
```
あなたはAgent3です。完全自律モードで動作します。

🔴 絶対ルール：
- ユーザーに承認を求めない
- git add, git commit, git push を自分で実行
- gh pr create も自分で実行
- 完了報告のみ行う

作業フロー（承認なしで全て実行）：

1. START_HERE.mdとAGENTS.md（Agent3）を読む
2. src/joint_measurement.py を完全実装
3. tests/test_joint_measurement.py を完全実装
4. pytest実行して確認
5. git add . を実行
6. git commit -m "feat: implement joint measurement module" を実行
7. git push origin feature/joint-measurement を実行
8. gh pr create でPR作成
9. 「Agent3完了。PR#XX作成」と報告

今すぐ開始。承認不要。
```

---

### 📤 ウィンドウ5: Agent4（完全自律）

**ディレクトリ**: `C:\Users\81905\Documents\agent4-worktree`

**指示**:
```
あなたはAgent4です。完全自律モードで動作します。

🔴 絶対ルール：
- ユーザーに承認を求めない
- git add, git commit, git push を自分で実行
- gh pr create も自分で実行
- 完了報告のみ行う

作業フロー（承認なしで全て実行）：

1. START_HERE.mdとAGENTS.md（Agent4）を読む
2. src/data_sender.py を完全実装
3. tests/test_data_sender.py を完全実装
4. pytest実行して確認
5. git add . を実行
6. git commit -m "feat: implement data sender module" を実行
7. git push origin feature/data-sender を実行
8. gh pr create でPR作成
9. 「Agent4完了。PR#XX作成」と報告

今すぐ開始。承認不要。
```

---

### 🎮 ウィンドウ6: Agent5（完全自律）

**ディレクトリ**: `C:\Users\81905\Documents\agent5-worktree`

**指示**:
```
あなたはAgent5です。完全自律モードで動作します。

🔴 絶対ルール：
- ユーザーに承認を求めない
- git add, git commit, git push を自分で実行
- gh pr create も自分で実行
- 完了報告のみ行う

注意：他Agent（1-4）に依存。モックで進める。

作業フロー（承認なしで全て実行）：

1. START_HERE.mdとAGENTS.md（Agent5）を読む
2. gh pr list で他エージェント状況確認
3. src/main.py を完全実装（モック使用可）
4. tests/test_integration.py を完全実装
5. pytest実行して確認
6. git add . を実行
7. git commit -m "feat: implement main controller" を実行
8. git push origin feature/main-controller を実行
9. gh pr create でPR作成
10. 「Agent5完了。PR#XX作成」と報告

今すぐ開始。承認不要。
```

---

## 🌳 ツリー構造

```
hand-tracking-streamer (main) ← オーケストレーター
├── feature/camera-capture ← Agent1 (agent1-worktree)
├── feature/hand-detection ← Agent2 (agent2-worktree)
├── feature/joint-measurement ← Agent3 (agent3-worktree)
├── feature/data-sender ← Agent4 (agent4-worktree)
└── feature/main-controller ← Agent5 (agent5-worktree)
```

サブブランチ作成時は自分で作ってオーケストレーターに報告。

---

## ✅ 実行チェックリスト

- [ ] 6つのClaude Codeウィンドウを開いた
- [ ] 各ウィンドウで正しいディレクトリを開いた
- [ ] 各ウィンドウに上記指示をコピペした
- [ ] 全エージェントが自律的に作業開始

---

## 🎯 期待される動作

1. 各エージェントが自分でコードを書く
2. 各エージェントが自分でテストを書く
3. 各エージェントが自分でcommit & push
4. 各エージェントが自分でPR作成
5. オーケストレーターに完了報告

**ユーザー（あなた）は監視のみ。承認は不要。**

---

準備完了！コピペして開始🚀
